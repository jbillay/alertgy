{
  "routes": [
    {
      "method": "GET",
      "path": "/allergens",
      "handler": "allergen.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/allergens/count",
      "handler": "allergen.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/allergens/:id",
      "handler": "allergen.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/allergens",
      "handler": "allergen.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/allergens/:id",
      "handler": "allergen.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/allergens/:id",
      "handler": "allergen.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
